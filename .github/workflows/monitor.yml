#name: GR Bill Monitor
#on:
  schedule:
    - cron: '*/30 * * * *'  # каждые 30 мин
  workflow_dispatch:       # кнопка Run

#jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install
      run: pip install feedparser python-telegram-bot requests
      
    - name: Run Bill Monitor
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
#      run: python monitor_rss.py
